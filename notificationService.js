// notificationService.js
function sendNotification(userId, message, d) {

    // Your logic to send the notification to the user with the provided userId and message
    console.log(`Sending notification to user ${userId}: ${message} and data ${d}`);

}

module.exports = { sendNotification };